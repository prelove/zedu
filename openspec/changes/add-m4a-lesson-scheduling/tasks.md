## 1. 契约与迁移

- [x] 1.1 冻结 M4a 排课边界：仅课程排期事实、审计与查询；不含通知、考勤、财务、结款或冲突增强。
- [x] 1.2 新增 `006_m4a_lesson` migration，并以 SQLite 的 up/down/up 测试验证。

## 2. 后端课程排期领域

- [x] 2.1 实现创建课程：仅 Owner/Operator、ACTIVE 报名与 ACTIVE 师生安排可创建；课程事实与审计在同一事务中提交。
- [x] 2.2 实现输入校验：10–480 分钟、IANA 时区、当地时间转 UTC、WeChat 链接校验。
- [x] 2.3 实现更新、取消、详情和列表：仅 `SCHEDULED` 可变更或取消；支持学生、老师、状态与时间范围筛选。

## 3. 前端课程页面

- [x] 3.1 新增认证保护的 `/lessons` 路由、导航入口、API adapter 和三语词条。
- [x] 3.2 实现创建表单并提交冻结的 M4a 排课字段。
- [x] 3.3 实现列表、筛选、分页与仅 `SCHEDULED` 状态可取消的操作。

## 4. 验证与发布门禁

- [x] 4.1 后端：课程 HTTP 生命周期、权限、时区、状态机、无财务副作用和 migration 测试通过。
- [x] 4.2 前端：adapter 契约测试、lint、typecheck、Vitest 和构建通过。
- [x] 4.3 OpenSpec strict 验证通过；未发现阻断 M4a 的 P0/P1 问题。

## 5. 跟踪与下一里程碑交接

- [x] 5.1 生成 M4a 证据、追踪说明与状态同步记录。
- [x] 5.2 写明 M4b 仅可在 M4a lesson 事实基础上增加 Resend outbox，不得回写 M4a 行为。
